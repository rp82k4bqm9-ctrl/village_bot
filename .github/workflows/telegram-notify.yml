name: Telegram Notify

on:
  push:
    branches: [main, master]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send to Telegram
        run: |
          curl -s -X POST "https://api.telegram.org/bot8534730006:AAGIMjk0a459q_zMzV3kLMxJyvkwHTlsrcI/sendMessage" \
            -d "chat_id=6153426860" \
            -d "text=🚀 Пуш от ${{ github.actor }}: ${{ github.event.head_commit.message }}"
