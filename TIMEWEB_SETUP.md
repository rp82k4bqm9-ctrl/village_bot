# 🐘 Настройка PostgreSQL на Beget для мини-приложения

Этот файл описывает, как поднять базу PostgreSQL на Beget и подключить к текущему проекту (Vercel + `DATABASE_URL`).

## Шаг 1: Создание базы данных на Beget

1. Зайди в панель управления Beget: `https://cp.beget.com`
2. Перейди в раздел **"Базы данных"**
3. Нажми **"Создать базу данных"**
4. Выбери:
   - **Тип:** PostgreSQL
   - **Имя базы:** `village_store` (можно своё)
   - **Имя пользователя** и **пароль** запиши отдельно
5. Сохрани настройки

> Если Beget предлагает выбрать сервер/локацию — выбирай тот, что ближе к пользователям (обычно Москва/СПб).

## Шаг 2: Получение данных для подключения

1. В разделе **"Базы данных"** найди свою базу PostgreSQL
2. Открой её параметры (иконка "Инфо"/"Настройки")
3. Найди:
   - **Хост** (например, `pgsql-1.beget.com`)
   - **Порт** (обычно `5432`)
   - **Имя базы**
   - **Пользователь**
4. Собери строку подключения в формате:

```text
postgresql://USER:PASSWORD@HOST:PORT/DB_NAME
```

Пример:

```text
postgresql://village_user:strongpass@pgsql-1.beget.com:5432/village_store
```

Если Beget требует SSL для внешних подключений, добавь в конец:

```text
?sslmode=require
```

Итог:

```text
postgresql://village_user:strongpass@pgsql-1.beget.com:5432/village_store?sslmode=require
```

## Шаг 3: Инициализация таблицы

Нужно выполнить SQL из файла `database.sql`, который лежит в корне проекта.

### Вариант 1: Через phpPgAdmin / веб-интерфейс Beget

1. В панели Beget открой базу данных
2. Найди ссылку на **phpPgAdmin** или встроенный SQL‑редактор
3. Открой файл `database.sql` у себя на компьютере
4. Скопируй содержимое и вставь в SQL‑редактор
5. Нажми **Выполнить**

### Вариант 2: Через консоль с ПК

Если у тебя установлен `psql`:

```bash
psql "postgresql://USER:PASSWORD@HOST:PORT/DB_NAME?sslmode=require" -f database.sql
```

Подставь свои значения из шага 2.

## Шаг 4: Настройка переменных окружения на Vercel

1. Открой проект на `https://vercel.com`
2. Перейди в **Settings → Environment Variables**
3. Добавь переменные:

| Name           | Value                                              |
|----------------|----------------------------------------------------|
| `DATABASE_URL` | Строка подключения из шага 2 (с `sslmode=require`) |
| `ADMIN_TOKEN`  | `village-admin-2024` (или свой секретный токен)    |

4. Нажми **Save**
5. Перейди в **Deployments** и сделай **Redeploy** последнего деплоя

## Шаг 5: Проверка работы

1. Открой админ‑панель (`/admin`) в мини‑приложении
2. Обнови страницу
3. Если подключение к базе успешно:
   - Ошибок не будет
   - Можно добавлять/редактировать игры

Если видишь сообщение об ошибке подключения:

- Проверь правильность `DATABASE_URL` (логин/пароль, имя базы, хост, порт)
- Убедись, что база на Beget запущена и доступна

## Готово ✅

- ✅ Каталог и заказы хранятся в PostgreSQL на Beget  
- ✅ Админ‑панель работает поверх реальной базы  
- ✅ Никаких привязок к Timeweb в коде/настройках больше нет
