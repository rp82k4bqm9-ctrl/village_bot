# 🐘 Настройка Timeweb Cloud PostgreSQL

## Шаг 1: Регистрация

1. Перейди на https://timeweb.cloud
2. Зарегистрируйся (можно через Telegram, Google или email)
3. Подтверди телефон

## Шаг 2: Создание базы данных

1. В панели управления нажми **"Создать"** → **"База данных"**
2. Выбери:
   - **Тип:** PostgreSQL
   - **Версия:** 15 (или 14)
   - **Тариф:** Starter (от 200₽/мес) или тестовый период
   - **Имя базы:** `village_store`
   - **Регион:** Москва (ближе = быстрее)
3. Нажми **"Создать"**
4. Жди 5-10 минут пока база запустится

## Шаг 3: Получение данных для подключения

1. Открой созданную базу
2. Перейди во вкладку **"Подключение"**
3. Скопируй **"Строку подключения"** (Connection String)

Выглядит так:
```
postgresql://user:password@host:port/database
```

## Шаг 4: Инициализация таблицы

1. В панели Timeweb найди кнопку **"SQL запрос"** или **"phpPgAdmin"**
2. Выполни SQL из файла `database.sql` (в проекте)

ИЛИ через консоль:
```bash
psql "postgresql://user:password@host:port/database" -f database.sql
```

## Шаг 5: Настройка Vercel

1. Открой проект на https://vercel.com
2. Перейди в **Settings** → **Environment Variables**
3. Добавь переменные:

| Name | Value |
|------|-------|
| `DATABASE_URL` | Строка подключения из шага 3 |
| `ADMIN_TOKEN` | `village-admin-2024` |

4. Нажми **Save**
5. Перейди в **Deployments** и нажми **Redeploy** на последнем деплое

## Готово! ✅

Теперь:
- ✅ Данные хранятся на сервере (не в localStorage)
- ✅ Работает на всех устройствах
- ✅ Админ-панель синхронизируется
- ✅ Заказы сохраняются

## Цена

- **Тестовый период:** 3 дня бесплатно
- **Starter:** ~200-300₽/месяц (хватит на 1000+ игр)
- **Расширение:** При необходимости можно увеличить

## Поддержка

Если что-то не работает:
- Чат поддержки Timeweb (справа внизу в панели)
- Telegram: @timeweb_support
