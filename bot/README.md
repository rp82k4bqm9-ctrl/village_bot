# Telegram Бот для Village Gaming Store

Бот для просмотра каталога игр и оформления заказов.

## 🚀 Быстрый старт

### 1. Получи токен у @BotFather

1. Открой Telegram и найди `@BotFather`
2. Отправь `/newbot`
3. Придумай имя и username (должен заканчиваться на `bot`)
4. **Скопируй токен** (выглядит как: `123456789:ABC...`)

### 2. Настрой переменные окружения

```bash
cp .env.example .env
```

Открой `.env` и вставь свой токен:
```
BOT_TOKEN=твой_токен_от_botfather
API_URL=https://твой-сайт.vercel.app
```

### 3. Установи зависимости

```bash
pip install -r requirements.txt
```

### 4. Запусти бота

```bash
python bot.py
```

Бот должен написать "✅ Бот запущен!"

## 📁 Структура

```
bot/
├── bot.py              # Основной код бота
├── requirements.txt    # Зависимости Python
├── .env.example        # Пример переменных окружения
└── README.md           # Этот файл
```

## 🌐 Деплой бота

Бота нужно держать запущенным 24/7. Варианты:

### Вариант 1: Локальный компьютер
Просто запусти `python bot.py` и не выключай компьютер.

### Вариант 2: Облако (бесплатно)
- [Railway](https://railway.app) — проще всего
- [Render](https://render.com) — тоже просто
- [PythonAnywhere](https://www.pythonanywhere.com) — для Python

### Вариант 3: VPS (платно)
- DigitalOcean, Hetzner и т.д.

## 🛠 Команды бота

- `/start` — Главное меню
- `/catalog` — Каталог игр
- `/help` — Помощь

## 📦 Функции

- Просмотр каталога игр с API сайта
- Просмотр деталей игры (цена, описание, платформы)
- Оформление заказа (заглушка — нужно добавить реальную логику)
- Удобное меню с кнопками

## 🔧 Настройка заказов

Сейчас заказы просто показывают сообщение. Чтобы добавить реальную обработку:

1. Добавь таблицу `orders` в базу данных
2. Добавь API endpoint `/api/orders`
3. Измени функцию `order_game` в `bot.py` чтобы отправлять POST запрос

## 💡 Советы

- Не коммить файл `.env` с реальным токеном в git!
- Для production используй webhook вместо polling
- Добавь логирование заказов в админ-чат
