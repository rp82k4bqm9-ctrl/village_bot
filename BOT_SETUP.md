# 📋 Инструкция по привязке к другому боту

## Быстрая настройка

Все настройки бота находятся в файле `src/config/bot.ts`:

```typescript
export const BOT_CONFIG = {
  // Токен бота от @BotFather
  TOKEN: 'ВАШ_НОВЫЙ_ТОКЕН',
  
  // ID администраторов
  ADMIN_CHAT_IDS: [
    'ID_АДМИНА_1',
    'ID_АДМИНА_2',
  ],
  
  // URL мини-приложения
  WEB_APP_URL: 'https://ваш-новый-url.vercel.app',
};
```

## Шаги по смене бота

### 1. Создайте нового бота

1. Напишите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Укажите имя бота (например: "My Game Store")
4. Укажите username бота (например: "mygamestore_bot")
5. Получите токен (выглядит так: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2. Настройте бота

В @BotFather отправьте:
- `/setdescription` - описание бота
- `/setabouttext` - текст "О боте"
- `/setuserpic` - аватарка бота
- `/setcommands` - команды:
  ```
  start - Главное меню
  admin - Админ-панель
  id - Мой Telegram ID
  help - Помощь
  ```

### 3. Обновите конфигурацию

Откройте файл `src/config/bot.ts` и замените:

```typescript
TOKEN: '123456789:ABCdefGHIjklMNOpqrsTUVwxyz', // Новый токен
```

### 4. Добавьте админов

1. Попросите каждого админа написать новому боту `/id`
2. Получите их Telegram ID
3. Добавьте ID в список:

```typescript
ADMIN_CHAT_IDS: [
  '123456789',  // Админ 1
  '987654321',  // Админ 2
],
```

### 5. Обновите URL (если нужно)

Если вы разворачиваете на новом домене:

```typescript
WEB_APP_URL: 'https://ваш-новый-домен.vercel.app',
```

### 6. Пересоберите проект

```bash
npm run build
git add .
git commit -m "Update bot configuration"
git push
```

### 7. Настройте Webhook (опционально)

Для продакшена настройте webhook вместо polling:

```bash
# Установите webhook
curl -X POST "https://api.telegram.org/botВАШ_ТОКЕН/setWebhook?url=https://ваш-сервер.vercel.app/api/telegram"
```

## Проверка работы

1. Напишите новому боту `/start`
2. Проверьте, что открывается мини-приложение
3. Сделайте тестовый заказ
4. Проверьте, что заказ пришёл админам

## Важные замечания

- **Токен** - секретная информация, не публикуйте его!
- **ID админов** - узнать можно через команду `/id` у бота
- **URL** - должен быть с HTTPS
- **Vercel** - при деплое на Vercel URL будет вида `https://имя-проекта.vercel.app`

## Помощь

Если что-то не работает:
1. Проверьте токен в @BotFather
2. Убедитесь, что бот не выключен
3. Проверьте консоль браузера на ошибки
4. Проверьте логи сервера
